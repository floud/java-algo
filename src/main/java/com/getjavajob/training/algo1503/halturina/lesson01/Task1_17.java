package com.getjavajob.training.algo1503.halturina.lesson01;

/**
 * Created by floud on 15.03.2015.
 */
public class Task1_17 {

    private static double x;
    private static double a;
    private static double b;
    private static double c;

    public static void main(String[] args) {

        double o = Math.sqrt(1 - (1 - Math.cos(2*x))/2);  // o
        double p = 1 / (Math.sqrt(a * Math.pow(x, 2) + b * x + c)); // p
        double r = (Math.sqrt(x + 1) + Math.sqrt(x - 1)) / 2 * Math.sqrt(x); //r
        double s = Math.abs(x) + Math.abs(x + 1); //s


    }

}
